First have these two scripts along with the fasta files for analysis in the same folder.
Next, run the FreqCalculatorN.py script to get csv files containing amino acids frequencies per position
To create a sequence logos, run the RLogo.R in the same folder - it would automatically run on any csv file in the folder and create the sequence logos as png files.
